package com.ringforge.chord.events;

public enum EventType {
    RING_CREATED,
    NODE_JOINED,
    NODE_LEFT,
    KEY_STORED,
    KEY_DELETED,
    KEY_MIGRATED,
    LOOKUP_COMPLETED,
    RING_REPAIRED
}
